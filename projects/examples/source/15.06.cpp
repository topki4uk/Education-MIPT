////////////////////////////////////////////////////////////////////////////

// chapter : Networks

////////////////////////////////////////////////////////////////////////////

// section : Transmission Control Protocol

////////////////////////////////////////////////////////////////////////////

// content : Synchronous Server
//
// content : Sockets
//
// content : Internet Assigned Numbers Authority (IANA)
//
// content : Tool lsof

////////////////////////////////////////////////////////////////////////////

// support : sudo lsof -i :80
//
// support : sudo ./15.06

////////////////////////////////////////////////////////////////////////////

// support : https://www.iana.org/assignments/service-names-port-numbers

////////////////////////////////////////////////////////////////////////////

#include <vector>

////////////////////////////////////////////////////////////////////////////

#include <boost/asio.hpp>

////////////////////////////////////////////////////////////////////////////

int main()
{
    boost::asio::io_context context;

//  ------------------------------------------------------------------------

    boost::asio::ip::tcp::endpoint endpoint(boost::asio::ip::tcp::v4(), 80);

//  ------------------------------------------------------------------------

    boost::asio::ip::tcp::acceptor acceptor(context, endpoint, true);

//  ------------------------------------------------------------------------

    boost::asio::ip::tcp::socket socket(context);

//  ------------------------------------------------------------------------

    acceptor.accept(socket);

//  ------------------------------------------------------------------------

    std::vector < char > vector(1 << 10);

//  ------------------------------------------------------------------------

    boost::asio::read (socket, boost::asio::buffer(vector));

    boost::asio::write(socket, boost::asio::buffer(vector));
}

////////////////////////////////////////////////////////////////////////////